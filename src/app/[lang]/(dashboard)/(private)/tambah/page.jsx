// MUI Imports
import Grid from "@mui/material/Grid";

import CreateForm from "@/views/tambah";

const page = () => {
  return (
    <Grid container spacing={6}>
      <CreateForm />
    </Grid>
  );
};

export default page;
